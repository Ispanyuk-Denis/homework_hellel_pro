"use strict";

let userName = prompt("Привет! Как тебя зовут?");
alert (`Круто! Привет ${userName}!`);
let a = 2 ;
let b = 3 ;
alert (`Их сума равна ${a + b} !`);
alert (`Их разница равна ${a - b} !`);
alert (`Их умножение равно ${a * b} !`);
alert (`Их деление равно ${a / b} !`);
alert (`Возведение первой переменной в степень равную второй переменной будет равно ${a ** b} !`);